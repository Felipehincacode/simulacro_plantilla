{
  "name": "admin-cursos",
  "version": "1.0.0",
  "description": "SPA de gestión de cursos con autenticación",
  "main": "src/main.js",
  "scripts": {
    "server": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.0"
  }
}
